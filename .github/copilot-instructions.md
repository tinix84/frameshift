# GitHub Copilot instructions

Follow `AGENTS.md`. Keep provider SDK types outside the domain layer, validate all durable reasoning artifacts against `schemas/`, and add evaluation fixtures for contract changes. Never turn a model proposal into an approved decision without an explicit human checkpoint.
