{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://frameshift.dev/schemas/prompt-manifest.schema.json",
  "title": "FrameShift prompt manifest",
  "description": "The front matter of a prompt file. A prompt is a versioned interface, not free-form application code, so what it promises is declared and checkable. Required here are the three fields an execution request pins it by; the rest of #20's field list needs content decisions and is optional until those are taken.",
  "type": "object",
  "required": [
    "id",
    "version",
    "engine",
    "body_digest"
  ],
  "properties": {
    "id": {
      "$ref": "common.schema.json#/$defs/id"
    },
    "version": {
      "type": "string",
      "pattern": "^[0-9]+[.][0-9]+[.][0-9]+$"
    },
    "engine": {
      "type": "string",
      "description": "`shared` is a prompt no single engine owns, such as repair.",
      "enum": [
        "problem_framing",
        "causal_reasoning",
        "solution_generation",
        "decision_validation",
        "shared"
      ]
    },
    "output_schema": {
      "description": "A path under schemas/. Checked to exist.",
      "type": "string",
      "minLength": 1
    },
    "fixtures": {
      "description": "Evaluation cases that exercise this prompt. Each is checked to exist.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true
    },
    "repair_prompt": {
      "$ref": "common.schema.json#/$defs/id"
    },
    "invariants": {
      "description": "#20's required invariants and forbidden behaviors, stated as the prompt itself states them. An execution request pinning this prompt carries exactly this list, so an engine is told what the prompt promised and a request cannot quietly drop one.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 300
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "body_digest": {
      "description": "Digest of the prompt text below the front matter, with line endings normalized. It is what makes the version mean something: change the body without changing the version and this stops matching. The manifest does not digest itself, the same way a checkpoint does not carry its own digest.",
      "$ref": "common.schema.json#/$defs/digest"
    }
  },
  "additionalProperties": false
}
